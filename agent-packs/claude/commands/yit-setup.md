Set up and verify this YouTube Intel Tool clone on my machine.

Steps:
1. Run `pnpm run setup`.
2. Run `pnpm run doctor` and report any failing checks.
3. Confirm runtime status with `pnpm yit health` and `pnpm yit capabilities`.
4. If healthy, summarize exact local URLs (web app + metrics).
