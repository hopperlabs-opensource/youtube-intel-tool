export default function SafetyCheckPage() {
  return (
    <main className="mx-auto max-w-3xl p-6">
      <h1 className="text-2xl font-semibold text-zinc-900">Safety Gate Check</h1>
      <p className="mt-2 text-sm text-zinc-700">
        This page is intentionally minimal and exists for cross-browser validation of runtime safety notices and the
        local-use acknowledgement gate.
      </p>
    </main>
  );
}
